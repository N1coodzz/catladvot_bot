# 🐾 Домашние котики — Telegram-бот для пары

MVP Telegram-бота по ТЗ:

- 🐱 роли: Кот и Котик;
- 📝 бытовые задачи без жёстких категорий;
- ⭐ баллы и рейтинг заботы;
- 💬 мягкое обсуждение задач вместо “споров”;
- 📅 ручной график с несколькими периодами занятости в один день;
- 🌙 ночные и 12-часовые смены;
- 🔁 замены только для Кота;
- 🔎 шансы для обмена смены только для Кота;
- 💞 поиск совместного свободного времени;
- 🌤 утреннее сообщение с погодой через Open-Meteo без API-ключа;
- ⏰ плановые уведомления по Москве.

## Стек

- Python 3.11+
- aiogram 3
- SQLAlchemy async
- SQLite для MVP
- APScheduler
- Open-Meteo без API-ключа

## Быстрый запуск

1. Создай бота через BotFather и получи токен.
2. Скопируй `.env.example` в `.env`.
3. Заполни переменные:

```env
BOT_TOKEN=123456:ABC
WEATHER_CITY=Moscow
TIMEZONE=Europe/Moscow
DATABASE_URL=sqlite+aiosqlite:///./domashnie_kotiki.sqlite3
```

4. Установи зависимости:

```bash
python -m venv .venv
source .venv/bin/activate  # Linux / macOS
# .venv\Scripts\activate   # Windows

pip install -r requirements.txt
```

5. Запусти:

```bash
python -m app.main
```

## Патч 0.3

Добавлено и исправлено:

- удобное добавление графика через периоды занятости;
- можно добавить несколько периодов за один день, например `09:00–14:00` и `16:00–19:00`;
- при сохранении графика выбранный день перезаписывается целиком, чтобы не плодить дубли;
- после сохранения можно сразу посмотреть день или отредактировать его;
- для Котика из главного меню убраны недоступные “Замены” и “Шансы для обмена”;
- для Кота оставлены “Замены” и переименованные “Шансы для обмена”;
- добавлено объяснение разницы между заменами и шансами для обмена;
- вкладка “Баллы” теперь по умолчанию показывает день, а период можно переключить: день / неделя / месяц / всё время;
- добавлена история начислений за выбранный период;
- в сценариях добавлены подсказки, что делать дальше.

## Важно

Это MVP-реализация. Перед постоянным использованием нужно протестировать сценарии в Telegram, особенно графики, ночные смены, замены и пересечения свободного времени.
